apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calendar-scheduler
  template:
    metadata:
      labels:
        app: calendar-scheduler
    spec:
      containers:
        - name: scheduler
          image: thejubadze/calendar-scheduler:latest
